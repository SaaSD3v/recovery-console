# Channel recovery image build request.
# This builder intentionally accepts only the on-device validated base.
SOURCE_REF=Channel-Configs

# all | portrait-0 | landscape-90 | portrait-180 | landscape-270
ORIENTATION=all

# Fixed validated TeamWin base.
TWRP_VERSION=3.5.2_10-0

# Change to force a rebuild without changing configuration.
REQUEST_ID=1
